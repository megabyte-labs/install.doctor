[defaults]
ansible_managed = "Managed by Megabyte Labs via Ansible"
command_warnings = False
inventory = inventory
roles_path=~/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles:../

[winrm_connection]
scheme = https
server_cert_validation = ignore
transport = credssp,ssl
