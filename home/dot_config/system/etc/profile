# System-wide .profile for sh(1)

export PATH="$PATH:${HOMEBREW_PREFIX:-/opt/homebrew}/bin"

if [ -x /usr/libexec/path_helper ]; then
	eval `/usr/libexec/path_helper -s`
fi

if [ "${BASH-no}" != "no" ]; then
	[ -r /etc/bashrc ] && . /etc/bashrc
fi
